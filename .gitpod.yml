image:
  file: .gitpod.Dockerfile

tasks:
- name: Couchbase
  init:  echo "test"

# exposed ports
ports:
- port: 8091
  onOpen: open-preview

vscode:
  extensions:
    - redhat.java
    - vscjava.vscode-java-debug
    - vscjava.vscode-java-test
    - pivotal.vscode-spring-boot
